import React from 'react';
import { useLoaderData } from 'react-router';

const CoffeeDetels = () => {
    const coffeeData = useLoaderData();
    console.log(coffeeData)
  
    return (
        <>
        
        </>
    )
};

export default CoffeeDetels;